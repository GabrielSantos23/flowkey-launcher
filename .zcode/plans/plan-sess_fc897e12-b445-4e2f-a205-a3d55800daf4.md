Remove all i18n and make the app English-only (TS side only, per user's choice).

1. Enumerate all ~72 files importing from `src/services/i18n` and ~1000 `t()` call sites; identify dynamic-key call sites for manual handling.
2. Write a one-off codemod (Node, run once, not committed) that replaces `t('key')` / `t('key', {params})` with inline English strings (template literals for interpolation) using en.json, across TSX/TS. Prettier-format all touched files.
3. Manually fix any dynamic-key call sites and JSX edge cases.
4. Delete `src/services/i18n/` (service, index, 2 tests), `src/locales/en.json`, `src/locales/pt-BR.json`; remove `i18nService` import/init from `routes-react/LauncherPage.tsx` and `SettingsPage.tsx`; strip i18n stubs from `ShortcutCapture.render.test.tsx` and `ExtensionsTab.render.test.tsx`.
5. Sweep for stragglers: grep for `services/i18n`, `i18nService`, `resolveLocalized`, `registerCatalog`, `computeCandidates`, `pt-BR`, `locales/`; check `get_system_locale` frontend callers.
6. Run full verification matrix: `pnpm format:check`, `pnpm check:design`, `pnpm -r --if-present test:run`; confirm no Rust changes needed.

No git operations (user commits). Rust locale/number-formatting code stays untouched.