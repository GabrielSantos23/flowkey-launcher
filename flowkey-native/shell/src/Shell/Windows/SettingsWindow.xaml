<Window x:Class="FlowKey.Shell.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://lepoco/wpfui"
        xmlns:uiD="clr-namespace:Wpf.Ui.Markup;assembly=Wpf.Ui"
        Title="FlowKey Settings"
        Width="980" Height="640"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        WindowStyle="None"
        ResizeMode="CanMinimize"
        Background="{DynamicResource WindowBackgroundBrush}"
        FontFamily="{DynamicResource AppFontFamily}">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <uiD:ControlsDictionary />
      </ResourceDictionary.MergedDictionaries>

      <SolidColorBrush x:Key="ToggleSwitchFillOff" Color="{StaticResource KeycapBackgroundColor}" />
      <SolidColorBrush x:Key="ToggleSwitchFillOffPointerOver" Color="{StaticResource KeycapBorderColor}" />
      <SolidColorBrush x:Key="ToggleSwitchFillOffPressed" Color="{StaticResource KeycapBorderColor}" />
      <SolidColorBrush x:Key="ToggleSwitchFillOffDisabled" Color="{StaticResource KeycapBackgroundColor}" />
      <SolidColorBrush x:Key="ToggleSwitchFillOn" Color="{StaticResource AccentColor}" />
      <SolidColorBrush x:Key="ToggleSwitchFillOnPointerOver" Color="{StaticResource AccentColor}" />
      <SolidColorBrush x:Key="ToggleSwitchFillOnPressed" Color="{StaticResource AccentColor}" />
      <SolidColorBrush x:Key="ToggleSwitchFillOnDisabled" Color="{StaticResource KeycapBorderColor}" />
      <SolidColorBrush x:Key="ToggleSwitchStrokeOff" Color="{StaticResource KeycapBorderColor}" />
      <SolidColorBrush x:Key="ToggleSwitchStrokeOffPointerOver" Color="{StaticResource KeycapBorderColor}" />
      <SolidColorBrush x:Key="ToggleSwitchStrokeOffPressed" Color="{StaticResource KeycapBorderColor}" />
      <SolidColorBrush x:Key="ToggleSwitchStrokeOffDisabled" Color="{StaticResource KeycapBorderColor}" />
      <SolidColorBrush x:Key="ToggleSwitchStrokeOnPointerOver" Color="{StaticResource AccentColor}" />
      <SolidColorBrush x:Key="ToggleSwitchStrokeOnPressed" Color="{StaticResource AccentColor}" />
      <SolidColorBrush x:Key="ToggleSwitchKnobFillOff" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchKnobFillOffPointerOver" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchKnobFillOffPressed" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchKnobFillOffDisabled" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchKnobFillOn" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchKnobFillOnPointerOver" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchKnobFillOnPressed" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchKnobFillOnDisabled" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchContentForeground" Color="{StaticResource TextPrimaryColor}" />
      <SolidColorBrush x:Key="ToggleSwitchContentForegroundDisabled" Color="{StaticResource TextTertiaryColor}" />

      <Style x:Key="SettingsNavButton" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FontSize" Value="{DynamicResource KindFontSize}" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="RadioButton">
              <Border x:Name="Chrome" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource RowCornerRadius}" Padding="{DynamicResource PanelRowPadding}" SnapsToDevicePixels="True">
                <ContentPresenter VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SurfaceBrush}" />
                  <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource RowSelectedBackgroundBrush}" />
                  <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="SettingsRow" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
        <Setter Property="CornerRadius" Value="{DynamicResource RowCornerRadius}" />
        <Setter Property="Padding" Value="12,9,12,9" />
        <Setter Property="Margin" Value="0,0,0,3" />
      </Style>

      <Style x:Key="ChromeButton" TargetType="Button">
        <Setter Property="Width" Value="34" />
        <Setter Property="Height" Value="26" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="Chrome" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource KeycapCornerRadius}">
                <TextBlock Text="{TemplateBinding Content}" FontFamily="{DynamicResource GlyphFontFamily}" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SurfaceBrush}" />
                  <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="CheckBox">
              <Border x:Name="Chrome" Width="16" Height="16" CornerRadius="3" Background="{DynamicResource KeycapBackgroundBrush}" BorderBrush="{DynamicResource KeycapBorderBrush}" BorderThickness="1">
                <TextBlock x:Name="Check" Text="&#xE73E;" FontFamily="{DynamicResource GlyphFontFamily}" FontSize="10" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="Opacity" Value="0.45" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style TargetType="PasswordBox">
        <Setter Property="Background" Value="{DynamicResource KeycapBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource KeycapBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="FontSize" Value="{DynamicResource FooterFontSize}" />
        <Setter Property="MinWidth" Value="260" />
      </Style>

      <!-- Slim dropdown matching the launcher's search-bar filter pill. -->
      <Style x:Key="SlimComboBox" TargetType="ComboBox">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="FontSize" Value="{DynamicResource SecondaryFontSize}" />
        <Setter Property="Width" Value="200" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ComboBox">
              <Grid>
                <ToggleButton x:Name="Toggle" Focusable="False" ClickMode="Press"
                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                  <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                      <Border Background="{DynamicResource KeycapBackgroundBrush}" BorderBrush="{DynamicResource KeycapBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource KeycapCornerRadius}" Padding="10,5,8,5" MinHeight="{Binding MinHeight, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <ContentPresenter Grid.Column="0" Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}" ContentTemplate="{TemplateBinding ContentTemplate}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                          <TextBlock Grid.Column="1" Text="&#xE70D;" FontFamily="{DynamicResource GlyphFontFamily}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="10,0,0,0" />
                        </Grid>
                      </Border>
                      <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                          <Setter Property="Background" Value="{DynamicResource RowSelectedBackgroundBrush}" />
                        </Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </ToggleButton.Template>
                </ToggleButton>
                <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                  <Border Background="{DynamicResource WindowBackgroundBrush}" BorderBrush="{DynamicResource KeycapBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource KeycapCornerRadius}" Margin="6" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="320">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                      <ItemsPresenter />
                    </ScrollViewer>
                  </Border>
                </Popup>
              </Grid>
              <ControlTemplate.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="Opacity" Value="0.45" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
          <Setter.Value>
            <Style TargetType="ComboBoxItem">
              <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
              <Setter Property="FontSize" Value="{DynamicResource SecondaryFontSize}" />
              <Setter Property="Padding" Value="10,6" />
              <Setter Property="Cursor" Value="Hand" />
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Chrome" Background="Transparent" Padding="{TemplateBinding Padding}">
                      <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsHighlighted" Value="True">
                        <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource RowSelectedBackgroundBrush}" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="RecorderBox" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource KeycapBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource KeycapBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,5" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="IsReadOnlyCaretVisible" Value="False" />
        <Setter Property="FontSize" Value="{DynamicResource FooterFontSize}" />
        <Setter Property="MinWidth" Value="170" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
      </Style>

      <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource KeycapBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource KeycapBorderBrush}" />
        <Setter Property="FontSize" Value="{DynamicResource KindFontSize}" />
        <Setter Property="Padding" Value="10,3" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="Chrome" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource KeycapCornerRadius}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource RowSelectedBackgroundBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                  <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="Foreground" Value="{DynamicResource TextTertiaryBrush}" />
                  <Setter TargetName="Chrome" Property="Opacity" Value="0.6" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </ResourceDictionary>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="40" />
      <RowDefinition Height="1" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Background="{DynamicResource ChromeBackgroundBrush}" MouseLeftButtonDown="OnChromeDrag">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
        <TextBlock Text="Settings" FontSize="{DynamicResource HeaderFontSize}" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0" />
        <Button Style="{StaticResource ChromeButton}" Content="&#xE72B;" Click="OnBackClick" IsEnabled="{Binding CanNavigateBack, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
        <Button Style="{StaticResource ChromeButton}" Content="&#xE72A;" Click="OnForwardClick" IsEnabled="{Binding CanNavigateForward, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
      </StackPanel>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,6,0">
        <Button Style="{StaticResource ChromeButton}" Content="&#xE712;" ToolTip="Not available yet" IsEnabled="False" />
        <Button Style="{StaticResource ChromeButton}" Content="&#xE921;" Click="OnMinimizeClick" />
        <Button Style="{StaticResource ChromeButton}" Content="&#xE8BB;" Click="OnCloseClick" />
      </StackPanel>
    </Grid>

    <Border Grid.Row="1" Background="{DynamicResource DividerBrush}" />

    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="240" />
        <ColumnDefinition Width="1" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Background="{DynamicResource WindowBackgroundBrush}" Padding="10,14,10,10">
        <DockPanel>
          <Border DockPanel.Dock="Top" Background="{DynamicResource SurfaceBrush}" BorderThickness="0" CornerRadius="{DynamicResource RowCornerRadius}" Padding="8,5" Margin="0,0,0,12" ToolTip="Not available yet">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="{DynamicResource GlyphFontFamily}" FontSize="{DynamicResource GlyphFontSize}" Foreground="{DynamicResource TextTertiaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
              <TextBox Grid.Column="1" x:Name="SearchBox" IsEnabled="False" Text="Search settings…" Foreground="{DynamicResource TextTertiaryBrush}" Background="Transparent" BorderThickness="0" Padding="0" />
            </Grid>
          </Border>

          <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="NavPanel" />
          </ScrollViewer>
        </DockPanel>
      </Border>

      <Border Grid.Column="1" Background="{DynamicResource DividerBrush}" />

      <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Padding="24,18,24,18">
        <ContentControl x:Name="PageHost" Focusable="False" />
      </ScrollViewer>
    </Grid>

    <Grid x:Name="ModalOverlay" Grid.RowSpan="3" Visibility="Collapsed">
      <Border Background="{DynamicResource WindowBackgroundBrush}" Opacity="0.72" MouseLeftButtonDown="OnModalBackdropClick" />
      <Border x:Name="ModalCard" Background="{DynamicResource ActionPanelBackgroundBrush}" BorderBrush="{DynamicResource KeycapBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RowCornerRadius}" Width="480" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="18,14,18,14" />
    </Grid>
  </Grid>
</Window>
