<Window x:Class="FlowKey.Shell.Windows.ActionPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Actions"
        Width="420" SizeToContent="Height"
        MaxHeight="420"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Background="Transparent"
        AllowsTransparency="True"
        FontFamily="{DynamicResource AppFontFamily}"
        Deactivated="OnDeactivated">
  <Border CornerRadius="{DynamicResource RowCornerRadius}" BorderThickness="1" BorderBrush="{DynamicResource KeycapBorderBrush}" Background="{DynamicResource ActionPanelBackgroundBrush}" Margin="6">
    <ListBox x:Name="ActionsList"
             Background="Transparent"
             BorderThickness="0"
             Foreground="{DynamicResource TextPrimaryBrush}"
             KeyDown="OnKeyDown"
             MouseDoubleClick="OnDoubleClick"
             PreviewMouseLeftButtonDown="OnPreviewMouseDown">
      <ListBox.ItemContainerStyle>
        <Style TargetType="ListBoxItem">
          <Setter Property="Padding" Value="10,7" />
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="ListBoxItem">
                <Border x:Name="Bd" Background="Transparent" CornerRadius="{DynamicResource KeycapCornerRadius}" Padding="{TemplateBinding Padding}">
                  <ContentPresenter />
                </Border>
                <ControlTemplate.Triggers>
                  <Trigger Property="IsSelected" Value="True">
                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource RowSelectedBackgroundBrush}" />
                  </Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </ListBox.ItemContainerStyle>
    </ListBox>
  </Border>
</Window>
