// Legacy path — kept for import compatibility after the React migration.
// The component itself lives in components/react/Feedback.tsx.
export { FormField as default, FormField, type FormFieldProps } from '../react/Feedback';
